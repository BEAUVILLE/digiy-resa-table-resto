name: Patch RESA no default and clear

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/patch-inscription-resa-clear-no-default.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('inscription-resa.html')
          html=p.read_text(encoding='utf-8')
          before=html
          html=html.replace('carte-tarif-resa-wave-visible-2026-05-29','carte-tarif-resa-sans-preselection-effacer-2026-05-29')
          html=html.replace('Les extras s’additionnent en sous-total, puis le paiement Wave affiche tout le détail.','Aucun tarif n’est précoché. Le client choisit volontairement, les extras s’additionnent, et il peut effacer en cas d’erreur.')
          html=html.replace('<button class="btn green" type="button" id="btnWhatsApp">💬 Envoyer la preuve / parler à JB</button>', '<button class="btn green" type="button" id="btnWhatsApp">💬 WhatsApp</button><button class="btn" type="button" id="btnSms">📱 SMS</button><button class="btn" type="button" id="btnClear">🧹 Effacer</button>')
          html=html.replace('Tout est affiché ici : montant, bénéficiaire Wave, numéro, référence et date du récap. Après paiement, envoie la preuve à JB.', 'Le message contient uniquement les choix sélectionnés, avec date, sous-total et total. Après paiement Wave, envoie la preuve à JB.')
          html=re.sub(r'select\("base",\s*resaPlans\[\d+\]\);\s*}\)\(\);', 'update();})();', html)
          inject='''function clearChoices(){state.base=null;state.boost=null;state.reseau=null;markActive("base","");markActive("boost","");markActive("reseau","");update();if(window.innerWidth<=820){setTimeout(()=>$('summaryCard').scrollIntoView({behavior:'smooth',block:'start'}),80)}}'''
          if 'function clearChoices()' not in html:
              html=html.replace('function buildMessage(){', inject+'function buildMessage(){')
          if 'btnSms' in html and '$("btnSms")' not in html:
              html=html.replace('$("btnWhatsApp").addEventListener("click",()=>openWhatsApp(buildMessage()));', '$("btnWhatsApp").addEventListener("click",()=>openWhatsApp(buildMessage()));$("btnSms").addEventListener("click",e=>copyText(buildMessage(),e.currentTarget));$("btnClear").addEventListener("click",clearChoices);')
          if html!=before:
              p.write_text(html,encoding='utf-8')
              print('RESA patched')
          else: print('No change')
          PY
      - run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add inscription-resa.html
          git commit -m "Remove RESA default selection and add clear"
          git push
